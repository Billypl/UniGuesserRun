# Frontend environment variables
# IMPORTANT: These should match HOST_IP and BACKEND_PORT from root .env
# Keep synchronized or delete this file when using Docker (it will use root .env)

VITE_HOST_IP=localhost
VITE_API_TARGET=https://localhost:5223
VITE_BACKEND_PORT=5223
